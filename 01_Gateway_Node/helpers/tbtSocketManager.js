import { fyersTbtSocket } from "fyers-api-v3";

// TBT socket is a strict function specifically made to handle the TBT (tick-by-tick) connection
// NOTE: TBT socket can only have one instance running at a time and runnning multiple instances in one program would result in blockade from fyers side
// we are flexible to subscribe upto 5000 symbols dynamically while the instance is running 

class tbtManager {
    constructor(parameters) {
        
    }
}